edge_length = int(input("Enter the length of a cube's edge: "))

surface_area = 6 * edge_length ** 2

print("The surface area of the cube is:", surface_area)# momentum.py

mass = float(input("Enter the object's mass in kilograms: "))
velocity = float(input("Enter the object's velocity in meters per second: "))

momentum = mass * velocity

print("The object's momentum is:", momentum, "kg·m/s")